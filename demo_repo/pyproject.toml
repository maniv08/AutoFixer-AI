[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "string-math-engine"
version = "0.1.0"
description = "Sample repository with seeded multi-stage bugs for AutoFixer AI hackathon evaluation"
dependencies = [
    "pytest>=8.0.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
